const config = {
  content: ['./src/**/*.{html,js,svelte,ts}'],

  theme: {
    theme: {
      extend: {
        fontFamily: {
          serif: ['Playfair Display', 'serif']
        }
      }
    }
  },

  plugins: []
}

module.exports = config
